# =============================================================================
# requirements.txt - Python 依賴套件清單
# AI 互動遊戲展場專案的 Python 依賴配置
# =============================================================================

# =============================================================================
# Web 框架與伺服器
# =============================================================================
fastapi                    # 高性能 Web API 框架
uvicorn[standard]          # ASGI 伺服器，支援 WebSocket 和 HTTP/2
jinja2                     # 模板引擎，用於 HTML 渲染
python-multipart           # FastAPI 檔案上傳支援

# =============================================================================
# 資料處理與科學計算
# =============================================================================
numpy<1.26                # 數值計算基礎庫，版本限制以相容性
pandas>=2.0.0,<2.2        # 資料分析和操作，版本範圍控制
opencv-python-headless     # OpenCV 電腦視覺庫（無 GUI 版本）
pillow                     # Python 影像處理庫 (PIL)

# =============================================================================
# 開發工具
# =============================================================================
python-dotenv             # 環境變數管理
pytest                    # 測試框架
pytest-asyncio            # pytest 非同步測試支援
httpx                     # HTTP 客戶端，用於測試和 API 調用

# =============================================================================
# AI/ML 依賴套件
# =============================================================================

# DeepFace 人臉分析框架
# 版本 0.0.85 提供穩定的人臉情緒辨識功能
deepface==0.0.85

# DeepFace 相關依賴
requests>=2.27.1         # HTTP 請求庫
gdown>=3.10.1            # Google Drive 下載工具
tqdm>=4.30.0             # 進度條顯示

# MediaPipe 電腦視覺框架
# 版本 0.10.11 支援 GPU 加速和手勢辨識
# 支援 Delegate.GPU 用於硬體加速
mediapipe==0.10.11